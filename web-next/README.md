# Balance Tower WebGL prototype

Independent Telegram Mini App prototype. It does not load Godot or its WebAssembly export.

- Blender-authored `zen_world.glb` is rendered by Three.js.
- Rapier2D owns the tower physics.
- The existing 720 x 1280 gameplay contract is retained.
- The prototype is deployed beside the current build at `/next/`.
